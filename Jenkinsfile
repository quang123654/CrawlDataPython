/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh '''
                python --version
                '''
            }
        }
    }
}

